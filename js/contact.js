import { formInteraction, tabNavigation, formValidation, formSubmission, validationRules } from './components/formManeger.js';

formInteraction();
tabNavigation();
formValidation(validationRules);
formSubmission(validationRules);
